<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/llAccountItemContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp"
    android:layout_marginBottom="8dp"
    android:background="@drawable/account_item_background_default"
    tools:backgroundTint="#E0E0E0"> <!-- Color de fondo por defecto para preview, se sobrescribirá -->

    <!-- Símbolo/Icono de la cuenta -->
    <ImageView
        android:id="@+id/ivAccountSymbol"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="16dp"
        android:layout_gravity="center_vertical"
        app:tint="?attr/colorOnSurfaceVariant"
        tools:srcCompat="@drawable/baseline_account_balance_24" /> <!-- Icono por defecto para preview -->

    <!-- Contenedor para Nombre y Monto -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_gravity="center_vertical">

        <TextView
            android:id="@+id/tvAccountName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceTitleMedium"
            android:textStyle="bold"
            tools:text="Banco Principal" /> <!-- Ejemplo para preview -->

        <TextView
            android:id="@+id/tvAccountBalance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="?attr/colorPrimary"
            tools:text="$1,250.75 USD" /> <!-- Ejemplo para preview -->
    </LinearLayout>

    <!-- Indicador de color (opcional, otra forma de mostrar el color si no es el fondo) -->
    <!-- <View
        android:id="@+id/viewAccountColorIndicator"
        android:layout_width="10dp"
        android:layout_height="match_parent"
        android:layout_marginStart="8dp"
        tools:background="#4CAF50" /> -->

</LinearLayout>
